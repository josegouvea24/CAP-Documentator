// @ts-nocheck
sap.ui.define([
  "./BaseController"
], function (BaseController) {
  "use strict";

  /**
   * App Controller — root controller for the shell view.
   */
  return BaseController.extend("sap.capdocumentator.fioriui.controller.App", {

    onInit: function () {
      // Optional: preload models, check user roles, etc.
    }

  });
});